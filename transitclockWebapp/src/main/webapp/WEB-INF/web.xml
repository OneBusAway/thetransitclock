<?xml version="1.0" encoding="UTF-8"?>
<web-app xmlns="https://jakarta.ee/xml/ns/jakartaee"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="https://jakarta.ee/xml/ns/jakartaee
                             https://jakarta.ee/xml/ns/jakartaee/web-app_6_0.xsd"
         version="6.0">
  <display-name>The Transit Clock webapp</display-name>


  <context-param>
      <param-name>transitime_config_file_location</param-name>
      <!-- Set to customized properties file with db config info and such -->
      <param-value>${transitclock.configFiles}</param-value>

  </context-param>

  <!-- App-wide JSTL fmt resource bundle. Sets the default LocalizationContext
       at application scope so every JSP and tag file can use <fmt:message>
       without each one calling <fmt:setBundle>. Pages may still override at
       page scope if they want a different bundle. -->
  <context-param>
      <param-name>jakarta.servlet.jsp.jstl.fmt.localizationContext</param-name>
      <param-value>org.transitclock.i18n.text</param-value>
  </context-param>


  <!-- App-wide JSP defaults: UTF-8 source + response charset, and a prelude
       JSPF that registers the t/c/fmt taglib prefixes for every page. New
       pages only need <%@ page import="..." %> directives that are actually
       page-specific. -->
  <jsp-config>
    <jsp-property-group>
      <url-pattern>*.jsp</url-pattern>
      <page-encoding>UTF-8</page-encoding>
      <default-content-type>text/html; charset=UTF-8</default-content-type>
      <include-prelude>/WEB-INF/jspf/prelude.jspf</include-prelude>
    </jsp-property-group>
  </jsp-config>

  <welcome-file-list>
    <welcome-file>welcome/index.jsp</welcome-file>
    <welcome-file>index.html</welcome-file>
    <welcome-file>index.htm</welcome-file>
    <welcome-file>index.jsp</welcome-file>
    <welcome-file>default.html</welcome-file>
    <welcome-file>default.htm</welcome-file>
    <welcome-file>default.jsp</welcome-file>
  </welcome-file-list>

  <filter>
    <filter-name>XSSFilter</filter-name>
    <filter-class>org.transitclock.xssfilter.XSSFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>XSSFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>
  <filter>
    <filter-name>WebLoggingFilter</filter-name>
    <filter-class>org.transitclock.utils.web.WebLoggingFilter</filter-class>
  </filter>
  <filter-mapping>
    <filter-name>WebLoggingFilter</filter-name>
    <url-pattern>/*</url-pattern>
  </filter-mapping>

    <listener>
        <listener-class>
            org.transitclock.web.ReadConfigListener
        </listener-class>
    </listener>

</web-app>
